version: '3.1'
services:
  headers:
    build: 
      context: ./../../
      dockerfile: ./docker/test/Dockerfile
    container_name: java_lib_test
    volumes:
      - ./../../:/app
    working_dir: /app
    command: mvn test
    